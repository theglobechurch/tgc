# The Globe Church

Site running on CraftCMS.

Sprinkled with Tailwind.

## Setup

```bash
cp .env.example .env
```

Edit the `.env` file as required

```bash
composer install
npm install && npm run dev
```
